EVALUATE
TOPN(
    20,
    SUMMARIZECOLUMNS(
        'Adjuster'[Region],
        "TotalAmount", [Total Claim Amount],
        "ClaimCount", [Claim Count]
    ),
    [TotalAmount], DESC
)
ORDER BY [TotalAmount] DESC
