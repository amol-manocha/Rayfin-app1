EVALUATE
SUMMARIZECOLUMNS(
    'Claims'[Claim ID],
    'Customer'[Customer ID],
    'Customer'[Customer Name],
    'Claim Type'[Claim Type Name],
    'Claims'[Claim Status],
    "Amount", [Total Claim Amount]
)
ORDER BY 'Claims'[Claim ID] ASC
