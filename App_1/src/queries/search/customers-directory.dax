EVALUATE
SUMMARIZECOLUMNS(
    'Customer'[Customer ID],
    'Customer'[Customer Name],
    'Customer'[City],
    'Customer'[State],
    "ClaimCount", [Claim Count]
)
ORDER BY 'Customer'[Customer Name] ASC
